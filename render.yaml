services:
  - type: web
    name: softpro-server
    env: node
    plan: free
    region: oregon
    buildCommand: npm install && cd server && npm install
    startCommand: cd server && npm start
    envVars:
      - key: NODE_ENV
        value: production

